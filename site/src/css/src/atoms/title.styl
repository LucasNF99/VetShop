.a-title {
  font-size: 18px;
  letter-spacing: 0;
  padding: 16px;
  line-height: 1;
  &._main {
    font-size: 20px;
    font-weight: 500;
    padding: 0; 
    padding-left: 5px;
    color: #ffffff;
    text-shadow: 2px 1px $secondary;
  }
  font-family: $fontfamily-title;
}

.a-login_title {
  font-size: 28px;
  color: $secondary;
  font-weight: 500;
  font-family: $fontfamily-title;
  letter-spacing: -1px;
  text-shadow: 1px 1px $primary;
}